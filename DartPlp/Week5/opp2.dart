/*    // Named  Constructors
 const double xOrigin = 0;
 const double yOrigin = 0;

class Point{
  final double x;
  final double y;
  
  // set the x and y instance variables before the constructors
  Point(this.x, this.y);

  // Named constructor
  Point.origin()
    x = xOrigin;
    y = yOrigin;



} */
